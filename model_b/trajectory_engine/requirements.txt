# Trajectory Engine — Dependencies
# Pin ranges are intentionally loose; tighten for production.

# Core ML
ultralytics>=8.3.0          # YOLO11x (upgraded from YOLOv8n); YOLO11 requires >=8.3.0
opencv-python-headless>=4.9.0

# Event schema validation
pydantic>=2.5.0

# MQTT client
paho-mqtt>=2.0.0

# Numerics (already a transitive dep of ultralytics, listed for clarity)
numpy>=1.24.0

# Night/low-light enhancement — RetinexFormer_arch.py (ICCV 2023)
# Required only when ENABLE_NIGHT_ENHANCEMENT=True; harmless to install always.
einops>=0.7.0
